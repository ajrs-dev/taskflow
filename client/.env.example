# The client talks to the API through the Vite dev-server proxy (see
# vite.config.ts), so no API URL is needed for local development.
# This file exists as a placeholder for environment-specific settings.
